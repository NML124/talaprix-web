import { Route } from '@angular/router';
import { UsersFeatureAuth } from './users-feature-auth/users-feature-auth';

export const usersFeatureAuthRoutes: Route[] = [
  { path: '', component: UsersFeatureAuth },
];
