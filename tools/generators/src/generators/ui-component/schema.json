{
  "$schema": "https://json-schema.org/schema",
  "$id": "UiComponent",
  "title": "Talaprix UI component",
  "description": "Creates an exported standalone UI component with Talaprix defaults.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Component name.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "project": {
      "type": "string",
      "description": "Target Nx UI library (for example shared-ui-kit or products-ui).",
      "x-prompt": "Which UI library should contain the component?"
    },
    "directory": {
      "type": "string",
      "description": "Optional directory below src/lib."
    },
    "prefix": {
      "type": "string",
      "description": "Optional selector prefix. Defaults to the target project's prefix."
    },
    "skipFormat": {
      "type": "boolean",
      "description": "Skip formatting generated files.",
      "default": false
    }
  },
  "required": ["name", "project"]
}
