# talaprix-generators

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build talaprix-generators` to build the library.

## Running unit tests

Run `nx test talaprix-generators` to execute the unit tests via [Vitest](https://vitest.dev/).
